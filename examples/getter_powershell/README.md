# PowerShell Greeter

PowerShell module used by the module export example.

Run it directly:

```powershell
pwsh examples/getter_powershell/index.ps1 bob
```

Or dot-source it from another PowerShell script:

```powershell
. examples/getter_powershell/index.ps1
Greet bob
```
