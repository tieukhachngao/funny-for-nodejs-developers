# Assembly Greeter

Assembly module used by the module export example family.

Build and run it directly:

```bash
examples/greeter_assembly/build.sh bob
```

Output:

```bash
hello bob
```

The build script currently supports `darwin/x86_64` and `linux/x86_64` hosts with `clang`.
