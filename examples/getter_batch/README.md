# Batch Greeter

Windows Batch module used by the module export example.

Run it directly from `cmd.exe`:

```bat
examples\getter_batch\index.bat bob
```

The root `.bat` examples are Windows launchers for the matching `.sh` examples so every example keeps the same basename across languages.
