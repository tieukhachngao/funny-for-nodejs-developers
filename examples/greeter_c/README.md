# C Greeter

C module used by the module export example family.

Build and run it directly:

```bash
cc -std=c11 -Wall -Wextra -Werror examples/greeter_c/index.c -o /tmp/greeter_c
/tmp/greeter_c bob
```

Output:

```bash
hello bob
```
