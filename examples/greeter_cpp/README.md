# C++ Greeter

C++ module used by the module export example family.

Build and run it directly:

```bash
c++ -std=c++17 -Wall -Wextra -Werror examples/greeter_cpp/index.cpp -o /tmp/greeter_cpp
/tmp/greeter_cpp bob
```

Output:

```bash
hello bob
```
